sudo: required
language: node_js
node_js:
  - '8'
services:
  - docker

before_install:
  - docker build -t andreasgarvik/siamawanders -f Dockerfile.dev .
script:
  - docker run -e CI=true andreasgarvik/siamawanders npm run test --forceExit --watchAll=false
