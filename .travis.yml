sudo: required
services:
  - docker

before_install:
  - docker build -t andreasgarvik/siamawanders -f Dockerfile.dev .

script:
  - docker run -e CI=true andreasgarvik/siamawanders npm run test -- --watchAll=false
